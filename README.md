# 🐸 CURURU SOVADO (Godot 4.x)

This game was entirely generated by AI

"Is this much better than I could do by hand? Sure is." — torvalds

## 📁 Project Structure
- `res://src/` → game source code
- `res://assets/` → images, sounds, and music
- `res://project.godot` → Godot project file

## 🧩 Open In Godot
1. Open **Godot 4.x**.
2. Click **Import**.
3. Select this project folder (where `project.godot` is).
4. Confirm import and open the project.

## 🖥️ Run On Desktop
1. Open the main scene: `res://src/scenes/Main.tscn`.
2. Press **Play** (F5).

## 📦 Export to Android (.apk)
You will need:
- Godot 4.x export templates installed
- Android SDK + JDK configured
- A keystore (debug or release)

### 1) ✅ Install Export Templates
1. In Godot: **Editor > Manage Export Templates**.
2. Download and install templates for your Godot version.

### 2) 🛠️ Configure Android SDK/JDK
1. Go to **Editor > Editor Settings**.
2. Search for **Export > Android**.
3. Set paths for SDK, JDK, and tools (ADB, jarsigner, etc.).
4. Set a keystore under **Export > Android** (debug or release).

### 3) 🧱 Create Android Export Preset
1. Go to **Project > Export**.
2. Click **Add...** and choose **Android**.
3. Set:
   - **Package/Unique Name** (example: `com.yourname.cururusovado`)
   - **Version Code** and **Version Name**

### 4) 🚀 Export APK
1. In **Project > Export**, select the Android preset.
2. Click **Export Project**.
3. Choose output name and location.

## 🎨 Replace Images or Sounds Safely
You can replace any file inside `res://assets/` without breaking the build **as long as you keep the same filename and extension**.  
Example: keep `frog.png`, `chapel.png`, `melodia.mp3` with the same names.

## 📝 Notes
- Android icons may be cached by the OS. If you change the icon, uninstall the app before reinstalling.
